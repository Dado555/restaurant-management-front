import { OrderStatusDisplayPipe } from './order-status-display.pipe';

describe('OrderStatusDisplayPipe', () => {
  it('create an instance', () => {
    const pipe = new OrderStatusDisplayPipe();
    expect(pipe).toBeTruthy();
  });
});
