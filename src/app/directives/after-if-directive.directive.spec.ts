import { AfterIfDirectiveDirective } from './after-if-directive.directive';

describe('AfterIfDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new AfterIfDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
